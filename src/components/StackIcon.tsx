import React from "react";

interface StackIconProps {
  label: string;
  icon: JSX.Element;
}

const StackIcon: React.FC<StackIconProps> = () => {
  return <div>StackIcon</div>;
};

export default StackIcon;
